import base from './base';

const overrides = {
  name: 'simplify',
  color: 'blue.500',
  firstLabel: 'Max for the numerator',
  secondLabel: 'Max for the denominator',
};

export default base(overrides);
